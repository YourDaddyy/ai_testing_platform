services:
  crm:
    image: crm-ai-platform:latest
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - ../.env.local
    volumes:
      - ../config.json:/app/config.json
    restart: unless-stopped
